const path = require('path');

function blar

module.exports = {
    urlPathFromFilePath,
    cssClassesFromUrlPath,
    cssClassesFromFilePath,
    flattenMarkdownData,
    setEnvironmentVariables
};